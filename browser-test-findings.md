## اختبار المتصفح المحلي — المرحلة الأولى

تم فتح `http://127.0.0.1:5173/register` بنجاح، وظهر نموذج إنشاء حساب المعلم دون أخطاء. تم ملء حساب اختبار محلي باسم `Local First Test` وبريد `local-first-test@example.com` دون أخطاء في الحقول. لم يتم الاحتفاظ ببيانات الحساب داخل النسخة النهائية؛ الاستخدام للاختبار المحلي فقط.
## اختبار المتصفح المحلي — المرحلة الثانية

تم إرسال نموذج التسجيل والانتقال مباشرة إلى لوحة التحكم باسم المعلم التجريبي. ظهرت اللوحة بلا شاشة تحميل عالقة، ثم فُتح نموذج إنشاء الصف بنجاح. هذا يؤكد أن الجلسة المحلية وبدء التطبيق يعملان قبل اختبار عمليات local-first نفسها.
## اختبار المتصفح المحلي — المرحلة الثالثة

تم إنشاء صف باسم `صف الاختبار المحلي`، وظهر فورًا في لوحة التحكم مع المادة والسنة وعدد الطلاب، دون انتظار طويل. المعرف المحلي للصف بدأ بـ `class-`، ما يثبت أن إنشاء الصف المحلي يعمل قبل المزامنة الخلفية.
## اختبار المتصفح المحلي — المرحلة الرابعة

تم فتح الصف المحلي بنجاح، ثم فتح تبويب التحليلات. ظهرت الرسوم البيانية لتوزيع الدرجات ومتوسط الفئات والنمو والملخص الشامل من snapshot محلي، مع عبارة واضحة بأن الحساب محليًا. لم تظهر شاشة تحميل مستمرة أو خطأ في التبويب.
## اختبار المتصفح المحلي — المرحلة الخامسة

ظهر تبويب التقارير الصفية بنجاح مع عبارة `محسوب محليًا` وأزرار تصدير CSV/PDF، دون انتظار endpoint التقارير. بعد العودة إلى تبويب الطلاب ظل الصف مفتوحًا وعرض القائمة المحلية بلا أخطاء.
## اختبار المتصفح المحلي — المرحلة السادسة

تم فتح نموذج الطالب وملء الاسم ورقم القيد وبيانات ولي الأمر والملاحظات بنجاح. واجهة الصورة الرمزية والمعلومات المحلية ظهرت دون أخطاء قبل إرسال النموذج.
## اختبار المتصفح المحلي — المرحلة السابعة

بعد إرسال الطالب ظهر العدد `1` والرسالة `تم الحفظ والمزامنة`. ثم فُتح تبويب السلوك وظهر الطالب في الرصد والمؤشر المحلي دون تأخير ملحوظ، ما يؤكد أن بيانات الطلاب والسلوك تُقرأ من snapshot المحلي.
## اختبار المتصفح المحلي — المرحلة الثامنة

بعد توسيع الطالب ظهرت أنواع السلوك الستة من snapshot المحلي. عند الضغط على `مشاركة متميزة (+2)` ظهرت رسالة `تم رصد السلوك محليًا`، وتغير المؤشر فورًا إلى نقطتين وسجل إيجابي واحد، مما يثبت التحديث المحلي الفوري مع استمرار المزامنة الخلفية.
## اختبار المتصفح المحلي — المرحلة التاسعة

تم فتح تبويب الحضور وظهرت جلسة اليوم والطالب من snapshot المحلي. بعد الضغط على حفظ الحضور ظهرت رسالة `تم حفظ الحضور ومزامنته` وتغير المؤشر مباشرة إلى `100%`، ما يؤكد أن حفظ الحضور لا ينتظر إعادة تحميل الصفحة.
## اختبار المتصفح المحلي — المرحلة العاشرة

فتح دفتر الدرجات أظهر الفئات والتقييمات الجاهزة محليًا. تم إدخال الدرجة `85` في أول خانة، ثم الخروج من الحقل والعودة إلى الطلاب دون شاشة تعليق. يعرض دفتر الدرجات نصًا واضحًا بأن الخانة تُحفظ محليًا ثم تُزامن دفعيًا.
## التحقق النهائي

نجح `npm run build` للواجهة، ونجح `node --check` لكل ملفات الخادم والمسارات والوسائط. اختبار التكامل الكامل على خادم محلي نظيف نجح في إنشاء المعلم والصف والطالب، تسجيل درجة 85، تسجيل سلوك، حفظ حضور 100%، إنشاء رسالة مع `client_message_id` وإعادة إرسالها دون تكرار، ثم قراءة تقارير الصف والطالب وsnapshot.

نتيجة الاختبار: `finalGrade=85`، `studentReportGrade=85`، `categoryAverage=85`، و`messagesWithClientId=1`. أكبر حزمة رسوم بيانية تُحمّل عند فتح التحليلات/التقارير فقط، بينما أصبح chunk صفحة الصف الأساسي حوالي 3.2 كيلوبايت قبل الضغط بدل تحميل الرسوم الثقيلة معه.
## اختبار المتصفح المحلي — المرحلة الحادية عشرة

تم فتح الإعدادات وتبويب النسخة الاحتياطية. ظهر `snapshot محفوظ` مع عدد الكاش و`0 عمليات معلقة`، كما ظهرت خيارات المزامنة: عند الطلب، يوميًا، أسبوعيًا، وشهريًا، مع زر مزامنة الآن ومسح بيانات الجهاز. هذا يؤكد أن إعدادات المزامنة المحلية متاحة فعليًا للمعلم.
