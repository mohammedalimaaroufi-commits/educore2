# تدقيق ميزات EduCore Manager

## الحالة الحالية

المشروع عبارة عن واجهة React/Vite وخادم Express مع SQLite محليًا وTurso/libSQL في الإنتاج. طبقة local-first موجودة بالفعل عبر IndexedDB وsnapshot/outbox، لذلك يجب أن تُحدّث أي جداول جديدة داخل snapshot أو تُحفظ محليًا قبل إرسالها للخادم.

## دفتر الدرجات

الفئات موجودة في `grade_categories`، والتقييمات التفصيلية في `assessments`، والدرجات في `grades`. الواجهة الحالية تعرض التقييمات مباشرة على مقياس الدرجة القصوى، بينما الحساب المحلي والخلّاف يحوّلان مجموع التقييمات داخل الفئة إلى نسبة مئوية ثم يطبقان الوزن. المطلوب هو الحفاظ على وزن الفئة كقيمة نهائية للفئة: الفئة المباشرة تُدخل درجتها من وزن الفئة، والفئة التفصيلية تجمع عناصرها إلى الدرجة نفسها، مع عرض صف ثانٍ يعرض وزن الفئة وإجماليها.

سيتم تنفيذ ذلك بإضافة نوع للفئة (`direct` أو `detailed`) وحقول وصفية اختيارية، مع جعل التقييم المنشأ تلقائيًا عند إنشاء الفئة تقييمًا مباشرًا/ملخصًا عند الحاجة، وإضافة endpoint لتغيير نمط الفئة وإدارة التقييمات التفصيلية. ستبقى الدرجات القديمة قابلة للقراءة.

## السلوك

`BehaviorTab` يعرض رصد السلوك داخل قائمة قابلة للفتح، ثم يعرض مؤشرًا منفصلًا أسفل الصفحة. سيتم إبقاء الرصد بنقرة واحدة، وإضافة ملخص صغير ملاصق لاسم الطالب يتضمن النقاط وعدد الإيجابي/السلبي وآخر التفاصيل، ثم إزالة الجدول المنفصل أو تحويله إلى إحصاء عام فقط حتى لا تتكرر المعلومة.

## الاشتراكات والعروض

مدة التجربة تقرأ حاليًا من `TRIAL_DAYS` مع قيمة افتراضية 14، لكن لا يوجد إعداد محفوظ في قاعدة البيانات أو لوحة المسؤول. أسعار الخطط ثابتة في `auth.js`، وصفحة المعلم لا تدعم سعرًا أصليًا مشطوبًا أو عرضًا فعّالًا. كما أن التفعيل نفسه يعتمد على طلب تحويل يدوي؛ لذلك سيضاف جدول إعدادات/عروض قابل للإدارة، وستُرجع `/auth/plans` الأسعار الفعلية والسعر الأصلي ونسبة العرض، بينما يبقى اعتماد طلب التحويل هو آلية التفعيل الحالية.

## إعادة تعيين كلمة المرور

التدفق الحالي يصدر token ويرسل الرابط تلقائيًا عبر SMTP أو يعيده في وضع التطوير. المطلوب هو إنشاء طلب في لوحة المسؤول بدل الإرسال التلقائي. سيُحفظ الطلب مع حالته، ويستطيع المسؤول إنشاء رابط لمرة واحدة ثم نسخه أو إرساله يدويًا إلى بريد المعلم. سيبقى endpoint reset-password موجودًا لكنه لن يُنشئ رابطًا من طلب المعلم مباشرة.

## اللغة

حقل `teachers.locale` موجود أصلًا ويُقبل في endpoint إعدادات الملف الشخصي، لكن لا يوجد مزود ترجمة أو مفتاح لغة. سيضاف `LocaleContext` بدكتشين عربي/إنجليزي، يضبط `dir` و`lang` ويخزن اختيار المعلم محليًا ثم يزامنه مع ملفه. ستبدأ الترجمة بالعناصر المشتركة والصفحات التي نعدلها في هذه المرحلة، مع إبقاء النصوص غير المرحّلة عربية بدل كسر الواجهة.

## ملفات التنفيذ الرئيسية

| المجال | الخادم | الواجهة |
|---|---|---|
| الدرجات | `backend/src/db/index.js`, `backend/src/routes/grades.js` | `GradeMatrix.jsx`, `CategoryManager.jsx`, `analyticsSelectors.js` |
| السلوك | `backend/src/routes/behavior.js` عند الحاجة | `BehaviorTab.jsx` |
| الاشتراك | `auth.js`, `admin.js`, `db/index.js` | `Subscription.jsx`, `AdminDashboard.jsx` |
| كلمة المرور | `auth.js`, `admin.js`, `db/index.js` | `ForgotPassword.jsx`, `AdminDashboard.jsx` |
| اللغة | `settings.js` و`auth.js` | `App.jsx`, `AuthContext.jsx`, `Settings.jsx`, `utils/i18n.js` |
| local-first | `sync.js` | `localDb.js`, `snapshotSync.js` عند إضافة حقول جديدة |
