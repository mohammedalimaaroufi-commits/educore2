# تشخيص الإنتاج

تم فحص `https://educore-qvxl.onrender.com/api/health` في 19 أغسطس 2026، وكانت الاستجابة:

```json
{"status":"ok","name":"EduCore Manager API","database":"turso"}
```

زمن الاستجابة المقاس من بيئة الاختبار كان تقريبًا بين 1.93 و3.11 ثانية حتى لنقطة health، ما يؤكد وجود كلفة اتصال/استيقاظ في الخدمة المنشورة. سبب عدم تحميل التحليلات والتقارير في المصدر المحلي هو الاستعلامات المتداخلة داخل `backend/src/routes/reports.js`: يتم تنفيذ استعلامات داخل حلقات الطلاب والفئات والتقييمات، مع استعلامات إضافية للسلوك والحضور لكل طالب. سبب تكرار الدردشة هو الجمع بين العرض المتفائل ورسالة `new_message` التي تعيدها الخوادم إلى غرفة المرسل.

تمت إضافة مسار `GET /api/sync/snapshot` لتحميل بيانات المعلم في رحلة واحدة، وتحويل التحليلات والتقارير والدرجات والسلوك والحضور إلى قراءة وحساب محليين من IndexedDB.
